provider: huggingface
model: mistralai/Mistral-7B-Instruct-v0.3
params:
  backend: pipeline
  task: text-generation
  device_map: auto

  # Generation
  max_new_tokens: 1024
  temperature: 0

  # Model loading (8-bit bitsandbytes)
  model_kwargs:
    quantization_config:
      load_in_8bit: true

